// src/components/PDF/PDFDownload/styles.js
// Note: These are regular CSS styles, not PDF styles
import { StyleSheet } from "@react-pdf/renderer"

export const styles = {
  container: {
    padding: "20px",
    textAlign: "center",
  },
  downloadLink: {
    textDecoration: "none",
    padding: "10px 20px",
    color: "#fff",
    backgroundColor: "#007bff",
    borderRadius: "4px",
    cursor: "pointer",
  },
}
